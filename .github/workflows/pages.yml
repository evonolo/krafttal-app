name: Prototyp auf GitHub Pages veröffentlichen

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/configure-pages@v5

      # Der Prototyp liegt im Unterordner krafttal-app/. Nur dieser Ordner wird
      # veröffentlicht, damit index.html direkt unter der Seitenwurzel liegt.
      - uses: actions/upload-pages-artifact@v3
        with:
          path: krafttal-app

      - id: deployment
        uses: actions/deploy-pages@v4
